import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.epicureos.sanluis',
  appName: 'epicureos-mobile',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
